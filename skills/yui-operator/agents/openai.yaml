interface:
  display_name: "Yui Operator"
  short_description: "Record Tasks and route only explicitly started work"
  default_prompt: "Use $yui-operator to preserve the user's exact Task intent. A creation-only request creates an unstarted Draft; route discussion or development through the owning Task Leader only when the user explicitly asks to start it."
